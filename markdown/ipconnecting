sudo ip link set l4tbr0 up

sudo ip addr replace 192.168.137.2/24 dev l4tbr0


sudo ip route del default via 192.168.55.100 dev l4tbr0 2>/dev/null || true

sudo ip route del default via 192.168.55.1 dev l4tbr0 2>/dev/null || true

sudo ip route replace default via 192.168.137.1 dev l4tbr0 metric 50

sudo systemd-resolve --interface=l4tbr0 --set-dns=8.8.8.8 --set-dns=1.1.1.1 -set-domain=~.

ip -br addr show l4tbr0
ip route
ip route get 8.8.8.8
